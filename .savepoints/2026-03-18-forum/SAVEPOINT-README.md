# Savepoint — 2026-03-18

Copy of project state before the next prompt. See `SAVEPOINT.md` in project root.

To restore: copy all files from this folder over the project root (keep your `.env`), then `npm install`, `npx prisma generate`, `npm run dev`.
